require "spec_helper"

describe Garner::Cache do
  subject do
    Garner::Cache
  end

  describe "fetch" do
    it "requires bindings, a key hash, and an options hash"

    it "requires a block"

    it "caches values across calls from different lines"

    it "does not cache nil results"
  end
end
